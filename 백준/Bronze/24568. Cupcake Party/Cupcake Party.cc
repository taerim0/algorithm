#include <iostream>

using namespace std;


int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int R, S;

    cin >> R >> S;

    R * 8 + S * 3 - 28 ? cout << R * 8 + S * 3 - 28 : cout << 0;
}